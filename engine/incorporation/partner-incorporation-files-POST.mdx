---
title: "Register an incorporation file"
openapi: "POST /partner/incorporation/{incorporationId}/files"
---

Links an already-uploaded Commenda file to the incorporation context and returns that `fileId`.

Use this endpoint before linking files to evidence requirements such as `passportScans` or `utilityBills`.

```json
{
  "fileId": 456
}
```

MIME compatibility is checked when the file is referenced by an intake update, not only when it is registered.
